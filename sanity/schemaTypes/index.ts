import { buildRequest } from "./buildRequest";
import { course } from "./course";
import { rifle } from "./rifle";
import { siteSettings } from "./siteSettings";

export const schemaTypes = [rifle, course, siteSettings, buildRequest];
